ul.radio-buttons {
  list-style-type: none;
}

div.field {
  margin: 20px;
}

label.primary {
  font-weight: bold;
}

label.secondary {
}

table.ratings tr td {
  text-align: center;
}

tr.scores td {
  color: #555;
  border-top: 1px solid #ddd;
  border-bottom: 1px solid #ddd;
  padding: 3px;
}
